<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yst.study" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!--
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->

        <!--
        <activity android:name=".book.part17.TabSample"> -->
        <activity android:name=".doit.book.chapter2.part4.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".book.part12.ListViewSample"/>
        <activity android:name=".book.part12.ListCustomViewSample"/>
        <activity android:name=".sample.webview.WebViewSample"/>
        <activity android:name=".sample.splash.SplashSample" />
        <activity android:name=".book.part20.FragmentSample" />
        <activity android:name=".book.part20.ReplaceFragmentSample" />

        <!-- support v7의 ActionBarActivity를 사용할때 테마 추가안하면 에러뜸 -->
        <activity android:name=".doit.book.part3.NewActivitySample" android:theme="@style/Theme.AppCompat" />
        <!--android:theme="@android:style/Theme.NoTitleBar.Fullscreen" 이 옵션은 검은색 바탕으로 전환시킴.-->
    </application>

</manifest>
